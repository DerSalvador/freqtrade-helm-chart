apiVersion: v1
kind: Service
metadata:
  name: freqtrade-clusterip-{{ .Release.Namespace }}
  labels:
    app: freqtrade-clusterip-{{ .Release.Namespace }}
spec:
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: {{ .Values.config.api_server.listen_port }}
    targetPort: api
  selector:
    app: freqtrade-{{ .Release.Namespace }}
# ---
# kind: Service
# metadata:
#   name: freqtrade-clusterip-bias
#   labels:
#     app: freqtrade-clusterip-bias
# spec:
#   type: ClusterIP
#   ports:
#   - protocol: TCP
#     port: {{ .Values.config.api_server.listen_port }}
#     targetPort: api-bias
#   selector:
#     app: freqtrade-bias
